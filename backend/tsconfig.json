{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "typeRoots": ["./node_modules/@types", "./src"],                                   /* Specify an output folder for all emitted files. */
  }
}
